version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: pg-container
    environment:
      POSTGRES_PASSWORD: admin
    ports:
      - "15432:5432" 
    restart: unless-stopped